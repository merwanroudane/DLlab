# Deep Learning Interactive Academy — pinned, stable versions (spec §70)
streamlit==1.63.0
numpy==2.4.6
pandas==3.0.5
plotly==7.0.0
scikit-learn==1.9.1
# Frameworks Academy (module 21): imported lazily inside the lessons that need them.
tensorflow==2.20.0
keras==3.15.1
# CPU build of PyTorch. If pip cannot find this exact build on your platform run:
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
pytest==9.1.1
